[project]
name = "canslim-etl"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pykrx==1.0.51",
    "finance-datareader>=0.9",
    "sqlalchemy>=2.0",
    "psycopg2-binary>=2.9",
    "python-dotenv>=1.0",
    "pandas>=2.0",
]

[tool.setuptools.packages.find]
where = ["."]
